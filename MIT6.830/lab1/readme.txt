$ cd [project-directory] #a project-directory means a directory which contains the build.xml
$ # run all unit tests
$ ant test #after finshed the whole simpledb
$ # run a specific unit test
$ ant runtest -Dtest=TupleTest # after every exercise
